<?xml version="1.0" encoding="UTF-8"?>
<!--
  ============================================================================
  history-service :8085 — lịch sử duyệt web và lịch sử tìm kiếm.
  ============================================================================

  SERVICE DUY NHẤT DÙNG NoSQL, và lựa chọn đó có lý do chứ không phải để cho
  đủ món. So sánh với ba service dùng PostgreSQL trong cùng hệ thống:

    Tính chất dữ liệu          history          auth / downloads / settings
    ────────────────────────   ──────────────   ───────────────────────────
    Tỉ lệ ghi/đọc              rất cao (mỗi     thấp (đăng nhập, đổi cài
                               lượt ghé một     đặt — vài lần mỗi phiên)
                               bản ghi)
    Cần giao dịch nhiều bảng   KHÔNG            CÓ
    Quan hệ với dữ liệu khác   không có         có (tài khoản, tệp)
    Vòng đời                   TỰ HẾT HẠN       vĩnh viễn
                               (90 ngày)
    Giá trị một bản ghi        gần bằng không   cao (mất là mất thật)

  Bốn điểm khiến MongoDB đúng ở đây:

  1. TTL INDEX. Mongo tự xoá bản ghi quá hạn, không cần job dọn dẹp. Với SQL
     phải viết một job cron, và một job cron bị quên chạy sẽ biến bảng lịch sử
     thành nơi lưu vĩnh viễn thứ lẽ ra phải biến mất — vừa tốn đĩa vừa là một
     vấn đề về quyền riêng tư.
  2. GHI KHÔNG CẦN GIAO DỊCH. Mất một lượt ghé thăm không ảnh hưởng tới gì cả.
     Trả cái giá của ACID cho dữ liệu như thế là trả cho thứ không dùng tới.
  3. LƯỢC ĐỒ MỀM. Trình duyệt sẽ còn ghi thêm trường (favicon, thời gian ở
     lại, thiết bị). Với SQL, mỗi trường là một migration; ở đây là không gì
     cả.
  4. GOM NHÓM BẰNG AGGREGATION PIPELINE. "Những trang ghé nhiều nhất tuần
     này" là một pipeline, không phải một câu GROUP BY trên bảng đang bị ghi
     liên tục.

  ĐIỀU KHÔNG ĐƯỢC QUÊN: Mongo ở đây KHÔNG có giao dịch nhiều tài liệu, và
  service này cố tình không cần tới. Ngày nào một tính năng cần điều đó, đó là
  dấu hiệu tính năng ấy không thuộc về service này.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.vnsearch</groupId>
		<artifactId>vnsearch-parent</artifactId>
		<version>0.0.1-SNAPSHOT</version>
		<relativePath>../../pom.xml</relativePath>
	</parent>

	<artifactId>history-service</artifactId>
	<name>history-service</name>
	<description>Lịch sử duyệt web và lịch sử tìm kiếm (MongoDB)</description>

	<dependencies>
		<dependency>
			<groupId>com.vnsearch</groupId>
			<artifactId>vnsearch-platform</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb</artifactId>
		</dependency>

		<!-- Testcontainers: test tích hợp chạy trên MongoDB THẬT.
		     KHÔNG dùng Mongo nhúng (flapdoodle): nó tải một bản nhị phân khác
		     bản chạy thật, và những thứ dễ sai nhất ở đây — TTL index, chỉ mục
		     văn bản, aggregation pipeline — chính là những thứ khác nhau giữa
		     các phiên bản. Một bài test xanh trên thứ mà môi trường thật không
		     dùng là một bài test nói dối. -->
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>mongodb</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
