/**
 * TODO (PHASE 7): Dang ky cac ipcMain.handle tuong ung voi API expose o
 * preload/index.ts:
 *   browser.newTab, browser.closeTab, browser.switchTab, browser.navigate,
 *   browser.goBack, browser.goForward, browser.reload, browser.onTabUpdate.
 *
 * Moi handler goi xuong tabManager.ts o tren.
 */
export function registerIpcHandlers(): void {
  // TODO: implement in PHASE 7
}
