/**
 * TODO (PHASE 8): Icon back/forward/reload/home. Back/forward doc trang
 * thai tu historyStore.ts (2 Stack tu cai dat).
 */
function NavigationButtons(): JSX.Element {
  return <div className="flex gap-1 text-gray-400">{'< > ⟳ ⌂'}</div>
}

export default NavigationButtons
