/**
 * TODO (PHASE 8): Thanh tab kieu Chrome (bo goc tren, tab active noi len),
 * doc/ghi tabStore.ts. Nut "+" de mo tab moi (goi browser.newTab qua preload).
 */
function TabBar(): JSX.Element {
  return (
    <div className="flex h-9 items-center bg-gray-200 px-2 text-sm text-gray-500">
      TabBar (PHASE 8)
    </div>
  )
}

export default TabBar
