/**
 * TODO (PHASE 8): Zustand store cho back/forward, dung 2 Stack TU CAI DAT
 * (class Stack co push/pop/peek/isEmpty rieng, KHONG dung array.push/pop
 * truc tiep) cho moi tab: backStack va forwardStack.
 *
 * Quy tac:
 *   - navigate(url moi): push URL hien tai vao backStack, CLEAR forwardStack.
 *   - goBack(): pop backStack -> push URL hien tai vao forwardStack.
 *   - goForward(): pop forwardStack -> push URL hien tai vao backStack.
 *
 * Day la mo hinh Stack kinh dien (LIFO) ap dung cho dieu huong trinh duyet.
 */
export {}
