/**
 * TODO (PHASE 8): Zustand store quan ly danh sach tab (id, title, url,
 * favicon, activeTabId). Goi browser.newTab/closeTab/switchTab qua
 * window.browser (preload) va dong bo voi ipcHandlers o main process.
 */
export {}
