# CHỖ GIỮ CHỖ CHO CỤM DEV. Giá trị nằm trong Git nên KHÔNG phải bí mật thật.
# Overlay prod xoá tệp này và yêu cầu tạo Secret bằng tay.
apiVersion: v1
kind: Secret
metadata:
  name: vnsearch-secret
  namespace: vnsearch
type: Opaque
stringData:
  ADMIN_API_KEY: "dev-only-key-doi-truoc-khi-chay-that"
  POSTGRES_PASSWORD: "dev-only-password"
  AUTH_DB_PASSWORD: "dev-only-password-auth"
  DOWNLOADS_DB_PASSWORD: "dev-only-password-downloads"
  SETTINGS_DB_PASSWORD: "dev-only-password-settings"
  BOOTSTRAP_ADMIN_PASSWORD: "dev-only-admin-password"
  FOOTBALL_API_KEY: ""
