package com.vnsearch.query;

/**
 * TODO (PHASE 4): Merge/intersect posting list - phan DSA dat gia cua
 * module truy van.
 *
 * intersect(a, b): thuat toan two-pointer, O(m+n), KHONG dung
 * HashSet.retainAll (se so sanh hieu nang trong docs/DSA-REPORT.md).
 * union(a, b): merge kieu merge-sort, O(m+n).
 *
 * Toi uu khi truy van nhieu term: sap xep cac posting list theo do dai
 * TANG DAN roi intersect lan luot (list ngan truoc -> giam so phep so
 * sanh, vi ket qua trung gian cang ve sau cang ngan).
 */
public class PostingListMerger {
    // TODO: implement in PHASE 4
}
